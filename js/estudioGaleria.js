var swiper = new Swiper('.swiper-container', {
    spaceBetween: 0,
    pagination: {
       el: '.swiper-pagination',
      // type: 'fraction',
    },
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  });



  // Prueba galeria dinamica lightbox via atributos


  // var imgs = document.querySelector('.galeria');


  // const activador = matchMedia('(min-width : 1224px)');

  // const changeSize = function(mql) {
      
  //   if (mql.matches) {
  //     console.log('funciona');
  //   } else {
  //     console.log('se mamo');
  //   }
  // }

  // activador.addListener(changeSize);
  // changeSize(activador);
